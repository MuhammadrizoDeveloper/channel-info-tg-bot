<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Channel Info Web App 🌐</title>
  <link rel="stylesheet" href="/css/tailwind.css">
</head>
<body class="h-screen flex flex-col justify-center items-center bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500">
  <form class="flex gap-2" action="/search" method="POST">
    <label class="w-[250px] input input-bordered flex items-center gap-2">
      <input type="text" name="inputValue" class="grow" placeholder="Search" />
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="w-4 h-4 opacity-70"><path fill-rule="evenodd" d="M9.965 11.026a5 5 0 1 1 1.06-1.06l2.755 2.754a.75.75 0 1 1-1.06 1.06l-2.755-2.754ZM10.5 7a3.5 3.5 0 1 1-7 0 3.5 3.5 0 0 1 7 0Z" clip-rule="evenodd" /></svg>
    </label>
    <button type="submit" class="w-24 px-4 bg-blue-600 hover:bg-blue-700 transition ease-in-out duration-100 text-white p-2 rounded-lg">Submit</button>
  </form>
  <div class="flex flex-row gap-[40px] items-center px-10 mt-5 w-auto h-[200px] bg-gray-400 rounded-xl bg-clip-padding backdrop-filter backdrop-blur-lg bg-opacity-40 border border-gray-10">
    <% if(channelInfo.available === "true") { %>
      <img class="w-[120px] h-[120px] max-w-full max-h-full rounded-2xl object-contain" src=<%= channelInfo.photoUrl  %> alt="Profile Photo">
      <div class="flex flex-col mt-5 justify-center items-center text-white">
        <h2 class="text-center font-bold text-2xl"><%= channelInfo.title %></h2>
        <a class="text-center opacity-65" href=<% `https://t.me/${channelInfo.username}` %>>@<%= channelInfo.username %></a>
        <p class="text-center text-lg"><%= channelInfo.subscriberCount %> subscribers</p>
      </div>
    <% } else if (channelInfo.available === "false") { %>
      <h2 class="text-lg text-white">The channel doesn't exist! ❌</h2>
    <% } else { %>
      <h2 class="text-lg text-white">Search results will be here 🔍</h2>
    <% } %>
  </div>
</body>
</html>